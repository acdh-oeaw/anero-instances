[project]
name = "anero-instances"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markdown>=3.9",
]

[dependency-groups]
dev = [
    "djlint>=1.36.4",
]
